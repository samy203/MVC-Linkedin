@model PersonViewModel
    @{
        AjaxOptions ajaxOpts = new AjaxOptions()
        {
            HttpMethod = "POST",
            UpdateTargetId = "addpersonal",
            InsertionMode = InsertionMode.Replace
        };
    }

    <div id="addpersonal">
        @Ajax.ActionLink("Add Connection","AddFriendPersonal","Person",new { ID=Model.ID,RequiredUserID=Model.RequiredUserID},ajaxOpts,new { @class= "btn btn-default msg-btn" })
      @*  <button type="submit" class="btn btn-default msg-btn">Add Connection</button>*@
    </div>